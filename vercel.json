{
  "buildCommand": "npx vite build && npx esbuild api/_handler.ts --bundle --platform=node --format=cjs --outfile=api/index.js --external:pg-native --external:bufferutil --external:utf-8-validate",
  "outputDirectory": "dist/public",
  "framework": null,
  "installCommand": "npm install",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/connect", "destination": "/api" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
